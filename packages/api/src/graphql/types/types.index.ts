import * as query from './query.type';
import * as mutation from './mutation.type';
import * as user from './user.type';
import * as board from './board.type';
import * as deck from './deck.type';
import * as card from './card.type';

export const types = [
    query,
    mutation,
    user,
    board,
    deck,
    card,
];
